/* Operadores Aritmeticos */

// Numeros de ejemplo: 83 & 51

a = 83;
b = 51;

// Operador + (Suma)
let suma = a + b;
console.log(suma);

// Operador - (Resta)
let resta = a - b;
console.log(resta);

// Operador * (Multiplicación)
let multiplicacion = a * b;
console.log(multiplicacion);

// Operador / (División)
let división = a / b;
console.log(división);

// Operador % (Modulo)
let modulo = a % b;
console.log(modulo);

// Operador ** (Exponenciacion)
let exponenciacion = a ** b;
console.log(exponenciacion);
console.log(Math.pow(a, b));
